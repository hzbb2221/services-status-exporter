url: "http://speech.opsnote.top/api/text-to-speech"
method: "POST"
headers:
  Content-Type: "application/json"
body: '{"text": "测试文本", "voice_type": "广告男声"}'
interval_seconds: 30  # 监控间隔时间（秒）
timeout_seconds: 10   # 请求超时时间（秒）
labels:
  env: "测试环境"
  service: "文字转语音服务接口"
  type: "api"
